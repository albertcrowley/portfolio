source "https://rubygems.org"

# Use the github-pages gem to manage all official Jekyll dependencies
gem "github-pages" #, "~> 226" # Use the latest stable version (e.g., 226 for current GitHub Pages build)
gem "jekyll-theme-dinky"

# Pin Jekyll if needed, otherwise it's handled by github-pages
# gem "jekyll", "~> 4.3"

# Remove the default theme if you are using a remote_theme in _config.yml
# gem "minima", "~> 2.5"
